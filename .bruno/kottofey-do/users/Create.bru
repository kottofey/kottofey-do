meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@user.com",
    "password": "admin@user.com",
    "role": "admin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
