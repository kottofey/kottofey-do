meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "email": "another_user@user.com",
    "password": "another_user@user.com",
    "roles": ["user", "admin"]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
